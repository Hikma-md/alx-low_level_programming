#include <stdio.h>
/**
 * main - prints nos of base 16
 *
 * Return : 0
 */
int main(void)
{
        int i;

        for (i = 8; i < 18; i++)
        {
                putchar(i);
        }
        for (i = 3; i < 6; i++)
        {
                putchar(i);
        }
        putchar('\n');
        return (0);
